# 当前仓库规范化开发短计划

**目标：** 在不开发新功能的前提下，规范当前仓库、统一正式目录并完成真实验证。

**需求依据：** `docs/需求/减脂记录Agent_PRD.md`

### 任务 1：整理文档与历史记录
- 文件：`README.md`、`开发短计划/公开仓库通用化_完成记录.md`
- 步骤：归档旧完成摘要；在 README 明确唯一源码位置、隐私目录和 Windows 验收状态。
- 验证：文档不包含公开版个人绝对路径，目录说明与实际一致。

### 任务 2：收纳启动日志
- 文件：`.gitignore`、`启动减脂仪表盘.bat`、`dashboard-start.ps1`、`tests/test_windows_launcher.py`
- 步骤：日志统一写入 `runtime/logs/`；PowerShell 自动创建目录；测试同步检查路径。
- 验证：自动化测试通过，旧根目录日志不再由新脚本生成。

### 任务 3：统一正式项目目录
- 文件：项目根目录、本地 Hermes 路由参考
- 步骤：将 `四周减脂记录Agent` 重命名为 `减脂记录Agent`；更新仍有效的本地路径引用；确认旧目录消失。
- 验证：新目录 Git 状态、远程、测试均正常，旧目录不存在。

### 任务 4：整体验证与提交
- 文件：全部本轮改动
- 步骤：运行全量测试、服务 HTTP 冒烟、敏感信息扫描、`git diff --check`；审阅 diff 后提交并推送 `main`。
- 验证：命令退出码为 0，远端 `main` 指向新提交。
