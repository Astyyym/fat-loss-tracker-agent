# 公开仓库通用化完成记录

以下工作已在此前版本完成：

- README 与 Prompt 改用项目根目录相对命令，公开文件不依赖维护者本机路径。
- 新增 `config.example.json`，周期和营养目标可由本地 `config.json` 配置。
- `config.json`、健康数据、摘要、卡片和备份由 Git 忽略。
- 后端、今日卡片、仪表盘和趋势参考线读取同一份配置。
- 项目公开名称调整为“减脂记录 Agent”。
- GitHub 仓库名称改为不绑定固定周期的 `fat-loss-tracker-agent`。

本文件只保存历史完成事实，不作为后续开发计划。当前计划见：

- `开发短计划/当前仓库规范化计划.md`
